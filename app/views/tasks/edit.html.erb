<% #provide(:title, "編集画面") %>
<h1>編集画面</h1>
<%= form_for @task do |f| %>
  <p>
    <%= f.label :タイトル %><br>
    <%= f.text_field :title %>
    <% if @task.errors.any? %>
    <%= @task.errors.messages[:title][0] %>
    <% end %>
  </p>
    <p>
    <%= f.submit "編集する" %>
    </p>
    <% end %>