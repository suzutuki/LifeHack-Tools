<script>
    const today = new Date(new Date().setHours(0, 0, 0, 0))
    const months = new Date(today.getFullYear(), today.getMonth() - 12, today.getDate())

    // ● カレンダー
    // 選択できない日付データ（自由に変更できます。）
    const disable_dates = ['']

    // カレンダーの日本語化
    flatpickr.localize(flatpickr.l10ns.ja);

    // カレンダーの表示
    flatpickr('#date-form', {
        // スマートフォンでもカレンダーに「flatpickr」を使用
        disableMobile: true,
        minDate: months,
        // 選択できない日付
        disable: disable_dates
    });
</script>
