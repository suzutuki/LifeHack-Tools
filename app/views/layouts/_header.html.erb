<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
      <%= csrf_meta_tags %>
      <%= stylesheet_link_tag    'application', media: 'all',
                                 'data-turbolinks-track': 'reload' %>
      <%= javascript_include_tag 'application',
                                 'data-turbolinks-track': 'reload' %>
      <script src="https://kit.fontawesome.com/c9ec8f1a4e.js" crossorigin="anonymous"></script>
    <!--CDN 経由で読み込む場合-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/
            font-awesome-animation/0.2.1/font-awesome-animation.min.css">
    <%= render 'layouts/shim' %>
  </head>
<body>
<header class="navbar">
  <div class="container">
    <nav>
      <%=link_to(content_tag(:i,'HOME',class:"fas fa-home fa-3x fas fa-heartbeat 
      faa-pulse animated-hover",style: "color: #505050;"),root_path)%>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li>
        <%=link_to "My ToDoリスト！", user_path(current_user),style: "background: rgba(250, 240, 230, 0.1);
        color: #505050; font-size: 28px;" %>
          </li>
          <li><%= link_to "ユーザー設定", edit_user_path(current_user), 
        style: "background: rgba(250, 240, 230, 0.1); font-size: 28px;"%></li>
           <li> 
              <%= link_to "ログアウト", logout_path, method: :delete, 
              style: "background: rgba(250, 240, 230, 0.1); font-size: 28px;"%>
           </li>
            <% else %>
          <!--<nav class="head-link">-->
          <li><%= link_to "ログイン", login_path, 
          style: "background: rgba(250, 240, 230, 0.1); font-size: 28px;"%></li>
          <li><%= link_to "ユーザー登録", signup_path,
            style: "background: rgba(250, 240, 230, 0.1); font-size: 28px;" %>
          </li>
        <% end %>
        </li>
      </ul>
    </nav>
  </div>
</header>