<!--モバイルメニューとヘッダー担当-->
<body>
<div id="global-container">
  <div id="container">
    <div class="mobile-menu__cover"></div>
    <div class="nav-trigger"></div>
    <header class="header">
      <div class="header__inner">
        <div class="logo item">
          <span class="logo__Flower">
            <%= link_to "LifeHack tools", task_path(current_user), class: "logo__Garden" %>
        </div>
        <nav class="header__nav">
          <ul class="header__ul">
            <% if logged_in? %>
              <li class="header__li item">
                <%= link_to "ToDo", task_path(current_user) %>
              </li>
              <li class="header__li item">
                <%= link_to 'MAC', mac_path(current_user) %>
              </li>
              <% if !guest_user %>
                <li class="header__li item">
                  <%= link_to 'Config', edit_user_path(current_user) %>
                </li>
              <% end %>
              <li class="header__li item">
                <%= link_to 'If-then', it_path(current_user) %>
              </li>
              <li class="header__li item">
                <%= link_to 'Exercise', hiit_path(current_user) %>
              </li>
            <% else %>
              <li class="header__li item">
                <%= link_to "Log in", login_path %>
              </li>
              <li class="header__li item">
                <%= link_to "Sign-up!", new_user_path %>
              </li>
            <% end %>
          </ul>
        </nav>
        <button class="mobile-menu__btn">
          <span></span>
          <span></span>
          <span></span>
        </button>
      </div>
    </header>
    <!--divが2つ閉じられていないのに注意-->
    <!--footerの前に閉じるのとnavの後とscriptの前に閉じる-->