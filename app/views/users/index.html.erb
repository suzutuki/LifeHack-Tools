<% if logged_in? %>
  <%= render 'home' %>
  <%= link_to "ToDoリスト！", task_path(current_user),
              class: "btn-sticky", style: "font-size: 28px; color: #333;" %>
  <%= link_to "Macの原則(目標設定)とは？", details_path,
              class: "btn-sticky", style: "font-size: 28px; color: #333;" %>
  <%= link_to "Macの原則(目標設定)", mac_path(current_user),
              class: "btn-sticky", style: "font-size: 28px; color: #333;" %>
  <%= link_to "Macの原則を設定", new_mac_path(current_user),
              class: "btn-sticky", style: "font-size: 28px; color: #333;" %>
  <%= link_to "Macの原則を編集", edit_mac_path(current_user),
              class: "btn-sticky", style: "font-size: 28px; color: #333;" %>
  <%= link_to "If-Thenテクニック", if_then_path(current_user),
              class: "btn-sticky", style: "font-size: 28px; color: #333;" %>
  </div>
  <% else %>
  <%= render 'home' %>
  <%= link_to "ユーザー登録する！", new_user_path, class: "btn-sticky",
              style: "font-size: 28px; color: #333;" %>
  </div>
<% end %>

